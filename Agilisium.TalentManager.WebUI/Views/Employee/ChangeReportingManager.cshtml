@model IEnumerable<Agilisium.TalentManager.WebUI.Models.EmployeeQuickModel>

@{
    ViewBag.Title = "ChangeReportingManager";
}

<h2 style="text-align:center">This page is under design</h2>

<p>
    @Html.ActionLink("Go Back to Employees List", "List")
</p>
@*
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeEntryID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReportingManagerID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeEntryID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReportingManagerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>*@
